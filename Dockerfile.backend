# 使用官方Node.js 18镜像作为基础镜像
FROM node:18-alpine

# 设置工作目录
WORKDIR /app/backend

# 复制package.json和package-lock.json
COPY backend/package*.json ./

# 安装依赖
RUN npm install

# 复制后端代码
COPY backend/ ./

# 暴露端口
EXPOSE 3001

# 启动命令
CMD ["node", "server.js"]